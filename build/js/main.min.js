"use strict";

$(document).ready(function () {
  function e() {
    var e = window.innerWidth > 1440 ? window.innerWidth : window.innerWidth > 640 ? 1440 : window.innerWidth > 320 ? 640 : 320,
      n = window.innerWidth / e;
    document.documentElement.style.zoom = n;
  }
  $(window).on("resize", e), $(window).on("load", e), setInterval(e, 100);
}), $(document).ready(function () {
  $(".header .burger").on("click", function () {
    $("body").toggleClass("menu-open"), $(".nav__item").removeClass("active"), $(".nav__submenu").hide(300);
  }), $(".nav__item_submenu .nav__link").on("click", function (e) {
    if (window.innerWidth > 640) return;
    var n = $(e.currentTarget).parent();
    n.hasClass("active") ? (n.removeClass("active"), n.find(".nav__submenu").hide(300)) : ($(".nav__item").removeClass("active"), $(".nav__submenu").hide(300), n.addClass("active"), n.find(".nav__submenu").show(300));
  }), $(window).on("resize", function () {
    window.innerWidth > 640 && ($("body").removeClass("menu-open"), $(".nav__item").removeClass("active"), $(".nav__submenu").hide(300), $(".nav__item").removeClass("active"));
  });
});